global:
  resolve_timeout: 5m

route:
  receiver: discord-nas
  group_by: ['alertname', 'instance', 'job']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  routes:
    - matchers:
        - severity="critical"
      receiver: discord-nas
      repeat_interval: 1h

receivers:
  - name: discord-nas
    discord_configs:
      # URL is read from a secret file — never stored in this config or .env.
      # Create: echo -n 'https://discord.com/api/webhooks/...' > secrets/discord_webhook_url.txt
      - webhook_url_file: '/run/secrets/discord_webhook_url'
        send_resolved: true
        title: >-
          {{ if eq .Status "firing" }}🔥 FIRING{{ else }}✅ RESOLVED{{ end }} [{{ .CommonLabels.alertname }}]
        message: |
          {{ range .Alerts }}
          **Alert:** {{ .Labels.alertname }}
          **Severity:** {{ .Labels.severity | toUpper }}
          **Instance:** {{ .Labels.instance | default "N/A" }}
          **Summary:** {{ .Annotations.summary }}
          {{ if .Annotations.description -}}
          **Detail:** {{ .Annotations.description }}
          {{ end -}}
          **Started:** {{ .StartsAt | since | humanizeDuration }} ago
          {{ end }}

inhibit_rules:
  - source_matchers:
      - alertname="PrometheusTargetDown"
    target_matchers:
      - alertname=~"NASMemoryPressure|NASHighLoad"
    equal: ['job']