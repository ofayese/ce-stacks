services:
  mcp-gateway:
    image: docker/mcp-gateway:v0.42.0
    command:
      - --servers=duckduckgo
      - --transport=streaming
    mem_limit: 512m
    cpu_shares: 512
    # cpus: removed - Synology DS723+ kernel lacks CFS scheduler cgroup (NanoCPUs unsupported)
    security_opt:
      - no-new-privileges:true
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    restart: unless-stopped
    # Host 8812 → container 8811 (parity with root compose.yaml)
    ports:
      - 10.0.1.15:8812:8811
    volumes:
      # /var/run socket context
      # Host-profile fix: dropped :rw → :ro (parity with ../compose.yaml).
      - /var/run/docker.sock:/var/run/docker.sock:ro
    # Healthcheck type: A — HTTP /health probe (same as root compose.yaml)
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8811/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 15s
    environment:
      - PUID=${PUID:-0}
      - PGID=${PGID:-0}
      - TZ=America/New_York
    labels:
      - com.centurylinklabs.watchtower.enable=true
