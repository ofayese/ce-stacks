# Dockhand Environment Variables
# =============================================================================
# This .env file is consumed ONLY when you run `docker compose -f
# dockhand/compose.yaml ...` directly (ad-hoc / dev use). In production the
# RC script `dockhand/scripts/dockhand-start.sh` manages the container
# lifecycle and supplies its own defaults -- this file is NOT read by it.
#
# Most ongoing configuration is handled via the Dockhand web UI (Settings menu).
# =============================================================================

# Synology DSM standard user/group IDs
# Keep at 0 (root) for bind-mount compatibility with DSM
PUID=0
PGID=0

# Timezone
TZ=America/New_York

# Stack root path -- points at the `stacks/` directory itself (matches init-nas.sh).
# Bind-mounted read-write at /app/stacks inside the Dockhand container.
# STACK_ROOT=/volume2/docker/ce-stacks/stacks

# Dockhand runtime data directory (db/, stacks/, git-repos/, icons/, ...).
# Bind-mounted at /app/data inside the container. Lives outside the repo so
# it survives `git clone`-style resets.
# DOCKHAND_DATA=/volume2/docker/dockhand

# Docker API version on this NAS. Verify with:
#   docker version --format '{{.Server.APIVersion}}'
# Default: 1.43 (matches Docker 24.x on Synology DSM 7.3)
DOCKER_API_VERSION=1.43

# Optional: Pin Dockhand image to specific version (instead of 'latest')
# DOCKHAND_IMAGE=fnsys/dockhand:v1.0.29

# Optional: Database backend (Dockhand supports SQLite or PostgreSQL)
# Default: SQLite at /app/data/db/dockhand.db
# DATABASE_URL=sqlite:/app/data/db/dockhand.db
# DATABASE_URL=postgresql://user:password@postgres:5432/dockhand

# Optional: Custom hostname for license validation (detected automatically)
# DOCKHAND_HOSTNAME=ds723

# Optional: Custom log level (debug, info, warn, error)
# LOG_LEVEL=info

# Synology-specific: Skip expensive disk usage collection (improves UI performance)
# This is set in the RC script and compose.yaml; no need to change here
SKIP_DF_COLLECTION=true
